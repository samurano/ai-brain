# Лог проекта `fitness-online/landing`

> Legacy note: в старых записях может встречаться путь `fitness-online-landing/*` как исторический контекст до миграции `HARNESS-2026-03-05-03`.

## 2026-02-28
- Создан отдельный проект под кодовую реализацию лендинга.
- Реализован статический фронтенд для GitHub Pages (`site/`):
  - `/` (предзапись),
  - `/pay` (оплата),
  - `/prechat-instruction`,
  - `/pay/success`,
  - `/privacy`.
- Добавлены JS-модули:
  - UTM first/last touch,
  - события аналитики,
  - ценовая логика и таймер,
  - mock API для тестирования UX без backend.
- Добавлен workflow деплоя GitHub Pages через Actions.
- Добавлен каркас Cloudflare Worker API (`workers/`) под интеграции:
  - prechat lead,
  - payment create,
  - payment webhook,
  - pricing current.
- Зафиксированы env-параметры и шаги интеграции в документации.
- [РЕШЕНИЕ ТРЕБУЕТСЯ] Подставить финальные Telegram-ссылки:
  - чат предзаписи,
  - закрытый чат после оплаты.
- [РЕШЕНИЕ ТРЕБУЕТСЯ] Перед запуском трафика завершить чековый контур YooKassa (54-ФЗ).
- Применены правки после внешнего экспертного ревью лендинга:
  - полностью обновлена структура и тексты страницы `/` (добавлены блоки «почему не получалось», «что изменится за 10 дней», расширенный FAQ, усиленные кейсы);
  - обновлена структура `/pay` (связка с бесплатными днями, блок ценности/гарантии, 6 вопросов FAQ, юридический блок продавца);
  - унифицированы CTA-кнопки на `/pay` через динамический `CURRENT_PRICE` для hero, секций и sticky-кнопки;
  - в `pay.js` добавлена синхронизация текстов CTA по цене и безопасная блокировка CTA при закрытых продажах.
- Принято решение: на текущем этапе оставляем примерные кейсы в прод-версии лендинга; замену на реальные отзывы делаем позже отдельной правкой контента.

## 2026-03-01
- Выполнен полный визуальный редизайн лендинга по ТЗ арт-директора (`/Users/deathindream/Documents/Редизайн лендинга Плоский живот v1.md`).
- Полностью пересобран `site/assets/css/styles.css`:
  - новая цветовая система через CSS-переменные;
  - mobile-first адаптив с брейкпоинтами 360/640/768/1024/1280;
  - обновленные компоненты (CTA-кнопки, карточки, бейджи, формы, таймер, ценовая лестница, FAQ-аккордеон, sticky CTA).
- Полностью обновлена верстка страниц `site/index.html` и `site/pay/index.html`:
  - внедрены новые секционные композиции, фото-заглушки, inline SVG-иконки, карусель кейсов на мобильных и адаптивные сетки.
- Обновлены технические страницы:
  - `site/prechat-instruction/index.html`;
  - `site/pay/success/index.html`.
- Переписана клиентская логика:
  - `site/assets/js/pages/home.js` — sticky CTA (IntersectionObserver), reveal-анимации, FAQ-аккордеон, dots кейсов, анимированное состояние успешной отправки формы;
  - `site/assets/js/pages/pay.js` — новый таймер (дни/часы/мин/сек), пульсация секунд, автообновление цены при переключении этапа, обновленная лестница цен, reveal/accordion/case-dots, sticky CTA.
- Бизнес-логика и аналитика не ломались: сохранены события, UTM, lead_id, mock/prod API-контракты, редиректы и поток оплаты.

## 2026-03-03
- По новой задаче редизайна (по свежим референсам) выполнен этап подготовки без имплементации:
  - создан `fitness-online-landing/docs/research.md` с deep research по структуре, ограничениям, точкам изменений и рискам;
  - создан `fitness-online-landing/docs/plan.md` с целью, acceptance criteria, пошаговым планом, файлами и TODO-чеклистом.
- Статус: STOP, ожидаем команду `GO` перед изменениями в верстке/стилях/JS.
- После команды `GO` выполнен редизайн страницы `/` по `docs/plan.md` в направлении `ice-blue / volumetric glass`:
  - подключено фото тренера `site/assets/img/IMG_5106.JPG` и использовано в hero + блоке эксперта;
  - полностью обновлена композиция первого экрана:
    - удален вторичный CTA в hero,
    - добавлен правый модуль прогресса `10 дней`,
    - добавлены 3 trust-карточки (`День 0 + день 1 бесплатно`, `15-20 минут в день`, `Безопасный старт`),
    - добавлена micro trust-строка `Ирина Денисова, тренер с опытом с 2011 года`;
  - в `site/index.html` добавлен class `page-home` для scoped-редизайна без влияния на `/pay`;
  - в `site/assets/css/styles.css` добавлен крупный scoped-блок `.page-home ...`:
    - новая палитра, стеклянные поверхности, объемные тени и фоновые градиенты,
    - редизайн карточек/форм/FAQ/кейсов/sticky CTA/footer в едином стиле,
    - адаптивные правила для hero и стеклянных модулей на `768+` и `1024+`.
- Проверено smoke-уровнем:
  - JS-критичные селекторы и id на `/` сохранены (`hero-cta`, `final-cta`, `prechat-form`, `sticky-cta`, FAQ, cases track);
  - `/pay` не трогался по разметке/логике.
- Self-review и точечные фиксы после редизайна:
  - исправлена HTML-запись URL Google Fonts (`&amp;` вместо сырого `&`);
  - для фото Ирины добавлены `width/height` в hero и expert-блок для снижения layout shift;
  - усилен контраст текста на `btn-primary` (обновлен градиент в `.page-home .btn-primary` и `:hover`, проверено по формулам контраста: min 4.70:1).
- Выполнена итерация V2 по замечаниям по визуалу:
  - добавлены модификаторы секций в `site/index.html` (`section--hero`, `section--glass-strong`, `section--glass-soft`, `section--plain`, `section--expert`);
  - полностью пересобран scoped-блок `.page-home` в `site/assets/css/styles.css`:
    - визуал смещен в сторону более яркого/объемного glass-референса;
    - усилены фоновые градиенты, стеклянные сферы и depth-слои;
    - внедрены токены ритма (`--space-section-y`, `--space-panel-pad`, `--space-card-pad`, `--space-stack`);
    - разделены типы секций `glass-soft` и `glass-strong` для устранения монотонности.
  - исправлен ритм:
    - уменьшены внешние расстояния между секциями;
    - увеличены внутренние отступы в карточках и списках;
    - стабилизирован вертикальный поток в контентных блоках.
  - устранен риск налезания текста на фото в блоке эксперта:
    - на `768+` установлена строгая 2-колоночная сетка;
    - добавлены ограничения текстовой колонки (`min-width: 0`, `max-width` абзацев).
  - обновлен градиент `btn-primary`/`:hover` для соответствия AA-контрасту на всем градиенте (минимум 4.67:1 в hover).
  - сохранены все рабочие JS-селекторы и бизнес-ограничения оффера на `/`; `/pay` не изменялся.
- Выполнен полный браузерный self-review после установки Playwright/Node (viewport: `320`, `768`, `1024`, `1440`) с фиксацией дефектов и точечными правками:
  - устранено обрезание trust-карточек в hero на планшетных ширинах:
    - hero-композиция на `768-1279` переведена в одноколоночный режим;
    - двухколоночный hero возвращается с `1280+`;
  - устранено налезание текста на фото в expert-блоке:
    - на `768-1023` expert переведен в 1 колонку;
    - для `1024+` оставлена 2-колоночная сетка с увеличенным зазором и `coach-photo { width: 100%; }`;
  - устранено перекрытие контента sticky CTA на планшете:
    - sticky CTA отключен на `768+` и убран дополнительный `padding-bottom` у `with-sticky`.
- Проведена повторная проверка после фиксов:
  - `heroOverflow=0` на `320/768/1024/1440`;
  - пересечение `h2` и фото в expert-блоке отсутствует (`overlap=false` на `768/1024/1280/1440`);
  - критичные JS-селекторы на `/` сохранены (`#hero-cta`, `#final-cta`, `#prechat-form`, `#sticky-cta`);
  - визуально подтверждены улучшения видимости блоков и ритма отступов.
- Мобильная микро-итерация по новым замечаниям (по скриншотам):
  - исправлен выход фото эксперта за пределы контейнера:
    - для `.section--expert` включен `overflow: hidden`;
    - для мобильного режима зафиксированы `coach-photo { width: 100%; max-width: 100%; margin: 0; }`.
  - упрощен блок «Кому подходит программа» на мобильном:
    - добавлен модификатор секции `section--audience`;
    - убрана лишняя визуальная вложенность у пунктов списков (плоские строки с разделителями вместо дополнительных «карточек в карточке»).
  - исправлен блок «Как это работает» на мобильном:
    - добавлен модификатор секции `section--method`;
    - для карточек шагов собран компактный верхний ряд `Шаг + иконка` в одной строке.
  - выполнен браузерный чек на `390x844` (Playwright): подтверждено устранение всех 3 замечаний.
- Desktop-итерация по новому багу hero:
  - причина: после предыдущего фикса hero оставался одноколоночным до `1280px`, из-за чего первый экран на desktop занимал лишнюю высоту и требовал скролл;
  - правки:
    - возвращена 2-колоночная hero-композиция начиная с `1024px`;
    - уплотнены desktop-параметры hero (меньше паддинги, уменьшены размеры заголовка/подзаголовка/траст-карточек, уменьшен progress ring);
    - уменьшена высота фото в hero на desktop через `aspect-ratio: 5/4`;
    - сохранен усиленный layout на `1280+` с аккуратным увеличением gap.
  - результаты замеров (viewport / hero height):
    - `1024x768`: `704px` (было `796px`);
    - `1280x800`: `736px` (было `911px`);
    - `1366x768`: `746px` (было `961px`);
    - `1440x900`: `836px` (было `1005px`).
  - визуально подтверждено: на desktop снова 2 колонки без ручного уменьшения масштаба браузера.

## 2026-03-05 (AGENTS Harness v2 rollout для nested repo)

### Task ID
- HARNESS-2026-03-05-02

### Что сделано
- Добавлен локальный `fitness-online-landing/AGENTS.md`.
- Добавлен `fitness-online-landing/docs/agent-checklist.md`.
- Зафиксирован локальный процессный контракт для nested repo:
  - `Research → Plan → STOP → GO`,
  - `Task ID` в `docs/research.md`, `docs/plan.md`, `log.md`,
  - guardrails по UX/flow-инвариантам (`селекторы`, `UTM/lead/payment`, `pricing/timer`, backend contracts).
- Полный mini-harness (`scripts/hooks/Makefile`) в nested repo не добавлялся (по решению).

### Что проверить
1. `fitness-online-landing/AGENTS.md` и `fitness-online-landing/docs/agent-checklist.md` присутствуют.
2. Checklist покрывает pre-flight, до/после `GO`, финальные проверки.
3. Код `site/*` и `workers/*` не менялся в рамках этой задачи.

## 2026-03-05 | HARNESS-2026-03-05-03
- Task ID: HARNESS-2026-03-05-03
- Что сделано:
  - лендинг переведён из legacy-пути `fitness-online-landing/*` в `fitness-online/landing/*`;
  - локальные `AGENTS.md` и `docs/agent-checklist.md` выровнены под единый git-контур;
  - `README.md` обновлён: standalone-репозиторий больше не используется;
  - process-записи в `docs/research.md` и `docs/plan.md` дополнены секциями миграции.
- Что проверить:
  - путь `fitness-online/landing/*` используется во всех новых задачах;
  - `scripts/harness_check.py` видит `fitness-online/landing/AGENTS.md` как обязательный;
  - деплой вынесен в отдельный follow-up task.
